import { Component } from '@angular/core';import { Goods } from '../goods.model';import { CollectionService } from '../shared/collection.service';@Component({  templateUrl: './market.component.html',})export class MarketComponent {  collections: Goods[] = [  ];  constructor(private _getCollection: CollectionService) {    this.collections = this._getCollection.getCols()  }  onAdd(item: Goods[]) {    this._getCollection.addToSel(item);  }  onAddMenu(t, d, p) {    if (t && d && p) {      this._getCollection.addToCols(t, d, p);    }  }}