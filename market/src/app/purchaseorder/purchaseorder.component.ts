import { Component } from '@angular/core';import { Goods } from '../goods.model';import { CollectionService } from "../shared/collection.service";@Component({  templateUrl: './purchaseorder.component.html',})export class PurchaseOrderComponent {  collections: Goods[];  price: number = 0;  constructor(private _getCollections: CollectionService) {    this.collections = this._getCollections.getSelected();    this.collections.forEach((v) => {      this.price += Number(v.price);    });  }  onRemove(i: number) {    this._getCollections.removeFromSel(i);    if (!this.collections.length) {      this.price = 0;    } else {      this.price -= this.collections[i].price;    }  }}